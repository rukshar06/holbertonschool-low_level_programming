/**
 * _puts - prints a string of a characters
 * @str: is the string to be printed
 *
 * Return: the string
 */
void _puts(char *str)
{
	char[] = "I do not fear computers. I fear the lack of them - Isaac Asimov";

	printf("%s\n", char);
	return (0);
}
