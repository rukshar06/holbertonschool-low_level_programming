#include "main.h"
/**
* _isdigit - checks for a digit
* @c: integer
*
* Return: 1 if c is a digit otherwise return 0
*/
int _isdigit(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (0);
	}
	else
		return (1);
}
